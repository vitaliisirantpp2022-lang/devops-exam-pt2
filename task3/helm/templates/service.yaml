apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.app.targetPort }}
      protocol: TCP
  selector:
    app: {{ .Release.Name }}
